services:
  tg-mcp:
    image: ghcr.io/myrikld/tg-mcp:latest
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8000
      - MCP_PATH=/mcp
    restart: unless-stopped
