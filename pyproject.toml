[project]
name = "tg-mcp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastmcp",
    "pydantic-settings",
    "telethon",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg",
    "bcrypt",
    "cryptography",
]
